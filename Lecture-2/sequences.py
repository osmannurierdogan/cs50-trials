name = ['Henry', 'Ron', 'Hermione']
print(name[0])

coordinateX = 10.0
coordinateY = 20.0

"""
    Date Structures
        list => sequence of mutable values
        tuple => sequence of immutable values
        set => collection of unique values
        dict => collection of key-value pairs

"""
coordinate = (coordinateX, coordinateY) # (10.0, 20.0)
